import React from "react";
import styled from "styled-components";

const Container = styled.div`
  justify-content: center;
  align-items: center;
  display: flex;
  font-size: 2rem;
`;

const Category = () => {
  return <Container>All Categories</Container>;
};

export default Category;
